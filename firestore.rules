rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ADVERTENCIA: Estas reglas son solo para desarrollo y son inseguras.
    // Permiten que cualquiera lea y escriba en toda tu base de datos.
    // Se deben implementar reglas seguras antes de la producción.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
