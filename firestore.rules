rules_version = '2';

// Allow read/write access to all users under development
// WARNING: Do not use these rules in production!
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
